server:
  port: 8082
  servlet:
    context-path: /api/workstudy

spring:
  application:
    name: workstudy-service
  datasource:
    url: jdbc:mysql://localhost:3306/green_channel?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 20040526
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: dev

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: is_deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      id-type: auto
  type-aliases-package: edu.greenchannel.workstudy.entity

logging:
  level:
    edu.greenchannel.workstudy: debug
