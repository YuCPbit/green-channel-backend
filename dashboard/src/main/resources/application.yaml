server:
  port: 8094
  servlet:
    context-path: /dashboard

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/green_channel_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 20040526
    driver-class-name: com.mysql.cj.jdbc.Driver


  websocket:
    enabled: true

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/**/*.xml

logging:
  level:
    edu.dashboard: debug
    com.alibaba.excel: info

easyexcel:
  ignore-empty-row: true