# 成员 A：7 月 15 日执行计划

日期：2026-07-15  
今日目标：项目能够启动，数据库能够初始化，五类用户具备最小登录和菜单隔离能力，其他成员能够基于公共规范并行开发。

## P0：必须完成

- [x] 确认需求范围、成员 A 职责和首日里程碑。
- [x] 初始化 Git `main` 分支和忽略规则。
- [x] 建立 Spring Boot 后端与 Vue 3 前端骨架。
- [x] 配置 MySQL 数据源并实测完整数据库脚本。
- [x] 建立五类开发账号和角色数据。
- [x] 实现登录、当前用户、注销和按角色返回菜单。
- [x] 建立统一 API 响应和全局异常处理。
- [x] 完成后端测试、前端生产构建和真实登录接口验证。

## P1：团队协作交付

- [x] 写明本地启动方式和开发账号。
- [x] 写明 API、错误码、分支、提交和数据库变更规范。
- [ ] 将首批 Story/Epic 录入 Jira，并替换 A/B/C/D 为真实姓名。
- [ ] 请 B/C/D 分别拉取项目并确认能启动；收集环境差异。
- [ ] 四人确认页面风格、审批状态枚举和附件接口边界。

## 今日验收记录

- 数据库脚本：成功执行，`green_channel` 共创建 40 张表。
- 初始化数据：五类账号均已写入，`user_type` 为 1—5。
- 后端测试：通过。
- 前端构建：通过。
- 健康检查：应用 `UP`、数据库 `UP`。
- 登录验证：正确密码返回学生菜单；错误密码返回 HTTP 401；令牌可读取当前用户。

## 明日衔接

7 月 16 日开始实现 GCS-S01 的完整 RBAC 数据驱动菜单，并推进新生信息、字典、附件和操作日志公共能力。临时内存令牌后续替换为 Redis 或统一身份认证适配器。
