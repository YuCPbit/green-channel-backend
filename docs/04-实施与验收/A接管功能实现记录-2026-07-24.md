# A 接管功能实现记录（2026-07-24）

> 状态：现行实施记录
> 最后核验：2026-07-24
> 记录人：A（YuchuanPu）
> 覆盖 Jira：SCU2026J02-15、16、17、45、54，以及整合任务 65—68

## 1. 本轮为什么由 A 接管

C 明确反馈绿色通道核心功能已完成，但资助方案和申诉协作任务来不及完成；仓库审查也证实
`subsidy-service` 中没有资助方案、申诉和满意度问卷的实体、表或接口。D 的最新
`dev/workstudy` 已实现批次、岗位、报名、录用、考勤、薪酬、评价和协议，但只有直接离岗接口，
没有“申请—审批—联动名额”的调岗/离岗闭环。因此由 A 按发布目录中的接管清单补齐。

## 2. 整合成员代码时发现并修复的问题

| 来源 | 发现的问题 | 本轮处理 |
| --- | --- | --- |
| C 后端 PR #14 | 缺少三个公共枚举；重复启动类和 WebConfig；配置硬编码数据库口令、JWT 和错误端口；MyBatis-Plus 3.5.10 不能在 Boot 4 自动装配 | 补公共枚举，删除重复配置，恢复环境变量配置；升级至 MyBatis-Plus 3.5.17 Boot 4 starter，并迁移 Service API 包名 |
| B 后端 PR #13/#16/#17 | PR #17 与 D 的勤工实现同时修改服务；旧手写 SqlSessionFactory 会覆盖 Boot 4 自动配置 | 保留 B 的资金台账、辅导员事务和评价，移除旧手写工厂，使用 Boot 4 starter 自动配置 |
| D `dev/workstudy` | 没有 PR；与 B 评价模块有 11 处冲突；部分提交删除分页配置 | 选择性合并业务实现，保留分页和 B 的评价能力，不整体覆盖集成分支 |
| C 前端 PR #2 | 提交了 `.vs` 本机工作区和二进制缓存 | 删除 `.vs/` 并加入 `.gitignore` |
| B 前端 PR #3 | 与 C 同时修改 `App.vue`、`api.js`；台账导出将 Token 拼进 URL | 合并双方菜单和 API；导出改用 `Authorization` 请求头 |

## 3. A 新增的功能闭环

### 3.1 资助方案（Jira 15）

- 学校端创建、修改草稿，配置资金来源、固定/区间金额、名额、期限和准入条件。
- 草稿可试运行估算、发布，已发布方案可下线。
- 学生只会查到当前有效且已发布的方案。

### 3.2 学生申诉和管理员处理（Jira 16、17）

- 仅允许当前学生针对本人出现“不通过”审核记录的绿色通道或补助申请申诉。
- 申诉窗口读取 `APPEAL_WINDOW_DAYS`，默认 3 天。
- 附件只能绑定当前用户已上传的附件，防止跨用户引用。
- 申诉自动回到作出“不通过”的辅导员/学院/学校节点。
- 支持待受理、处理中、申诉成立、驳回、退回补充，并记录处理人和结论。

### 3.3 调岗与离岗（Jira 45）

- 学生从本人当前在岗记录发起调岗或离岗申请，同一在岗记录禁止重复待办。
- 调岗必须选择不同且仍有名额的上架岗位。
- 审批通过后以事务方式释放旧岗位名额、占用新岗位名额、保存旧在岗历史并生成新在岗关系和协议。
- 离岗审批通过后才改变在岗状态并释放名额，替代原来无需审批的直接变更。

### 3.4 满意度问卷（Jira 54）

- 学校创建并发布指定业务/批次的 5 分制问卷。
- 学生在开放日期内填写一次评分和建议，数据库唯一键防重复提交。
- 管理端查看答卷数、平均分、1—5 分分布、近期建议和简单高频词。

## 4. 数据库和兼容性

- 完整建库脚本已加入 `gc_aid_plan`、`gc_appeal`、`gc_satisfaction_survey`、
  `gc_satisfaction_response`、`gc_work_study_movement`。
- 已存在开发库执行 `../03-数据库/迁移-2026-07-24-A接管功能.sql`，无需重建数据库。
- 迁移脚本同时补齐早期 `gc_work_study_hire` 缺失的 `salary_rate` 和新增菜单权限。

## 5. 当前验证和剩余风险

- `subsidy-service` 与 `workstudy-service` 分别在跳过测试条件下构建通过。
- 前端合并 B/C 页面及 A 新页面后，`npm run build` 通过。
- 还需要在一套真实 MySQL 数据库执行增量迁移，并以学生、辅导员、学院、学校四类账号跑接口联调。
- 现阶段优先完成功能，成员历史测试不作为合入阻塞项；但主分支最终验收前仍需补状态机和并发场景测试。
